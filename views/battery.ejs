<!DOCTYPE html>
   <html lang="en">
   <head>
       <meta charset="UTF-8">
       <meta name="viewport" content="width=device-width, initial-scale=1.0">
       <title>Laptop Battery Information</title>
       <style>
           .battery {
               width: 100%;
               height: 30px;
               border: 1px solid #000;
               border-radius: 5px;
               overflow: hidden;
           }
           .battery-level {
               height: 100%;
               transition: width 0.3s;
           }
           .green { background-color: green; }
           .orange { background-color: orange; }
           .red { background-color: red; }
       </style>
   </head>
   <body>
       <h1>Laptop Battery Information</h1>
       <p>Battery Level: <%= percent || 0 %>%</p>
       <div class="battery">
           <div class="battery-level <%= color || 'green' %>" style="width: <%= percent ? percent + '%' : '0%' %>;"></div>
       </div>
   </body>
</html>